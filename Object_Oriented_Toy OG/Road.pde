
class Road { // sets the class "Road"

  void roadDraw() { // draws the road that the player drives on
    // draws the road
    stroke(0);
    fill(150);
    rect(0, 90, width, 400);
  }
}
